print("Welcome to a simple polynomial function limit calculator!")
print('Only polynomial functions of the following type are accepted as of now: ax(^n) +/- (bx) +/- (c)')
print("Therefore, please cooperate with the calculator's current limits and don't input any invalid values. :)")

limit_a = int(input("Please enter your limit a value. The a value is the value your x tends to: "))
function_a = int(input("Please enter the first term of your function: "))
exponential = int(input('Is your function quadratic? If yes, insert the exponent of your first x. If not, insert 0: '))
operator = input("Please input a valid mathematical operator in the following format: + or -! If it is a simple linear function, with just a single term, then insert 0: ")

if operator not in ["+", "-", "0"]:
    print("Invalid mathematical operator.")
    exit()

function_b = int(input("Does your function have a b? If so, please enter it. If not, insert 0: "))
secondx = int(input("Does your function have a second x? If yes, enter 1. If not, enter 0: "))

if secondx not in [0, 1]:
    exit()

secondoperator = input("Does your function have an operator after your second x? If yes, enter + or -. If not, enter 0: ")

if secondoperator not in ["+", "-", "0"]:
    exit()

if secondoperator != "0":
    function_c = int(input("Enter your third term, the c of the function: "))

if function_b == 0 and operator == "0" and exponential == 0:
    print("This limit results into:", function_a * limit_a)
elif function_b == 0 and operator == "0" and exponential != 0:
    print("This limit results into:", function_a * (limit_a ** exponential))
elif function_b != 0 and operator == "+" and secondx == 0 and exponential == 0:
    print(function_a * limit_a + function_b)
elif function_b != 0 and operator == "+" and secondx == 0 and exponential != 0:
    print(function_a * (limit_a ** exponential) + function_b)
elif function_b != 0 and operator == "-" and secondx == 0 and exponential == 0:
    print(function_a * limit_a - function_b)
elif function_b != 0 and operator == "-" and secondx == 0 and exponential != 0:
    print(function_a * (limit_a ** exponential) - function_b)
elif function_b != 0 and operator == "+" and secondx == 1 and secondoperator == "0" and exponential == 0:
    print(function_a * limit_a + function_b * limit_a)
elif function_b != 0 and operator == "+" and secondx == 1 and secondoperator == "0" and exponential != 0:
    print(function_a * (limit_a ** exponential) + function_b * limit_a)
elif function_b != 0 and operator == "-" and secondx == 1 and secondoperator == "0" and exponential == 0:
    print(function_a * limit_a - function_b * limit_a)
elif function_b != 0 and operator == "-" and secondx == 1 and secondoperator == "0" and exponential != 0:
    print(function_a * (limit_a ** exponential) - function_b * limit_a)
elif function_b != 0 and operator == "+" and secondx == 1 and secondoperator == "+" and exponential == 0:
    print(function_a * limit_a + function_b * limit_a + function_c)
elif function_b != 0 and operator == "+" and secondx == 1 and secondoperator == "+" and exponential != 0:
    print(function_a * (limit_a ** exponential) + function_b * limit_a + function_c)
elif function_b != 0 and operator == "+" and secondx == 1 and secondoperator == "-" and exponential == 0:
    print(function_a * limit_a + function_b * limit_a - function_c)
elif function_b != 0 and operator == "+" and secondx == 1 and secondoperator == "-" and exponential != 0:
    print(function_a * (limit_a ** exponential) + function_b * limit_a - function_c)
elif function_b != 0 and operator == "-" and secondx == 1 and secondoperator == "+" and exponential == 0:
    print(function_a * limit_a - function_b * limit_a + function_c)
elif function_b != 0 and operator == "-" and secondx == 1 and secondoperator == "+" and exponential != 0:
    print(function_a * (limit_a ** exponential) - function_b * limit_a + function_c)
elif function_b != 0 and operator == "-" and secondx == 1 and secondoperator == "-" and exponential == 0:
    print(function_a * limit_a - function_b * limit_a - function_c)
elif function_b != 0 and operator == "-" and secondx == 1 and secondoperator == "-" and exponential != 0:
    print(function_a * (limit_a ** exponential) - function_b * limit_a - function_c)













program calculadora_de_limites;

var
  limite_a, primeirox, resultado, primeirotermo, segundotermo, terceirotermo, segundox : integer;
  operador1, operador2 : string;

begin
  resultado := 0;

  writeln('Qual o a do seu limite? Insira um número inteiro.');
  readln(limite_a);

  writeln('Qual o seu primeiro termo? O a da sua função. Insira um número inteiro.');
  readln(primeirotermo);

  writeln('A sua função tem um x depois do primeiro termo? Se sim, insira 1. Se não, insira 0.');
  readln(primeirox);

  writeln('Tem algum operador depois do seu primeiro x? Se sim, insira + ou -. Se não, insira 0.');
  readln(operador1);

  writeln('Se você respondeu que havia um operador, insira o seu segundo termo. Se não tiver nenhum termo, insira 0.');
  readln(segundotermo);

  writeln('Há um segundo x? Se sim, insira 1. Se não, insira 0.');
  readln(segundox);

  writeln('Há um operador depois do seu segundo x? Se sim, insira + ou -. Se não, insira 0.');
  readln(operador2);

  writeln('Se você respondeu que há um operador depois do seu segundo x, insira o seu terceiro termo. Se não, insira 0.');
  readln(terceirotermo);

  if primeirotermo <> 0 then
     resultado := resultado + primeirotermo;

  if (limite_a <> 0) and (primeirox <> 0) then
     resultado := resultado * limite_a;

  if (operador1 <> '0') and (segundotermo <> 0) and (segundox = 0) then
  begin
      if operador1 = '+' then
          resultado := resultado + segundotermo;
      if operador1 = '-' then
          resultado := resultado - segundotermo;
  end;

  if (segundox <> 0) and (operador1 <> '0') then
  begin
      if operador1 = '+' then
          resultado := resultado + (segundotermo * limite_a);
      if operador1 = '-' then
          resultado := resultado - (segundotermo * limite_a);
  end;

  if (operador2 <> '0') and (terceirotermo <> 0) then
  begin
      if operador2 = '+' then
          resultado := resultado + terceirotermo;
      if operador2 = '-' then
          resultado := resultado - terceirotermo;
  end;

  writeln('Resultado do limite: ', resultado);
end.